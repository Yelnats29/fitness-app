<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit A Workout</title>
</head>
<%- include('./partials/nav') %>

    <body>
        <h1>Edit A Workout!</h1>
        <form action="/workout/<%= workout._id %>?_method=PUT" method="POST">


            <div>
                <label for="workoutName">Workout Name:</label>
                <div>
                </div>
                <input type="text" name="workoutName" value="<%= workout.workoutName %>" id="workoutName" />
            </div>
            </br>
            <div>
                <label for="workoutFocus">Target Muscle:</label>
                <div>
                </div>
                <input type="text" name="workoutFocus" value="<%= workout.workoutFocus %>" id="workoutFocus" />
            </div>
            </br>
            <div>
                <label for="mainGoal">Main Goal:</label>
                <div>
                </div>
                <input type="text" name="mainGoal" value="<%= workout.mainGoal %>" id="mainGoal" />
            </div>
            </br>
            <div>
                <label for="equipment">Equipment Used:</label>
                <div>
                </div>
                <input type="text" name="equipment" value="<%= workout.equipment %>" id="equipment" />
            </div>
            </br>
            <div>
                <label for="sets"># of Sets:</label>
                <div>
                </div>
                <input type="number" name="sets" value="<%= workout.sets %>" id="sets" />
            </div>
            </br>
            <div>
                <label for="reps"># of Repetitions:</label>
                <div>
                </div>
                <input type="number" name="reps" value="<%= workout.reps %>" id="reps" />
            </div>
            </br>
            <div>
                <label for="description">Recent Comments: <%= workout.description %></label>
                <div><small>Enter anything more you'd like to share here</small>
                </div>

                <textarea id="description" name="description"  maxLength="300"></textarea>
            </div>

        </br>
            <button type="submit">Update <%= workout.workoutName %></button>

        </form>
    </body>

</html>